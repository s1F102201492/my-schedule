'use client'

import {createTheme} from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        main: '#6fbf73',
        light: '#ffffff',
        contrastText: '#000000'
      }
    },
  });
  
  export default theme;