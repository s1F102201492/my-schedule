'use client'

import {createTheme} from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        main: '#6fbf73',
        light: '#ffffff',
        dark: '#205723',
        contrastText: '#000000',
      }
    },
  });
  
  export default theme;